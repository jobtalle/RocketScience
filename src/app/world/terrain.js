/**
 * An environment to place bots in.
 * @param {Number} width The width in meters.
 * @constructor
 */
export function Terrain(width) {
    const _height = new Array(width * Terrain.SEGMENTS_PER_METER);
}

Terrain.SEGMENTS_PER_METER = 2;