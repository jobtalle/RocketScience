/**
 * @param {Object} behavior A behavior object matching this state object.
 * @constructor
 */
export function LedState(behavior) {
    /**
     * Update the state.
     * @param {Array} state A state array to read from and/or write to.
     */
    this.tick = state => {

    };
}